Gem::Specification.new do |spec|
    spec.name          = 'user_converter'
    spec.version       = '0.1.0'
    spec.authors       = ['Axel BUEE']
    spec.summary       = 'Converts user data from JSON to CSV'
    spec.description   = 'A simple Ruby library for converting user data from JSON to CSV.'
    spec.email         = 'axel.buee@gmail.com'
    spec.files         = Dir['lib/**/*']
  end